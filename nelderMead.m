%NELDER MEAD%
%FUNCTION (Intial simplex, paramter values,obj function)
%OUTPUTS
    %Yk at each itterate (stored in list)
    %f(y0) at at each itterate (store in list)
    %number of function evals [function eval,f(yk)]